[project]
name = "elite-system"
version = "0.1.0"
description = "Elite System - sistema comercial, industrial e auditavel"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
cloud = ["psycopg[binary]>=3.2"]

[project.scripts]
elite = "elite_system.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
